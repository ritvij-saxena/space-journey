{
  "name": "unsupervised-frontend",
  "version": "0.1.0",
  "description": "Generative art installation frontend - Unsupervised MoMA Replica",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "wasm:build": "cd ../rust-wasm && wasm-pack build --target web --release",
    "build:all": "npm run wasm:build && npm run build",
    "clean": "rm -rf dist node_modules/.vite"
  },
  "dependencies": {
    "axios": "^1.6.0",
    "detect-gpu": "^5.0.70",
    "stats-gl": "^4.1.0",
    "three": "^0.160.0"
  },
  "devDependencies": {
    "@rollup/pluginutils": "^5.3.0",
    "@vitejs/plugin-basic-ssl": "^1.0.1",
    "terser": "^5.46.0",
    "vite": "^5.0.0",
    "vite-plugin-glsl": "^1.3.0",
    "vite-plugin-top-level-await": "^1.4.1",
    "vite-plugin-wasm": "^3.3.0"
  }
}
